name: TodoFocusMac
options:
  minimumXcodeGenVersion: 2.38.0
  deploymentTarget:
    macOS: "14.0"
settings:
  base:
    SWIFT_VERSION: 6.0
    PRODUCT_BUNDLE_IDENTIFIER: com.michaelmjhhhh.todofocus.mac
packages:
  GRDB:
    path: ../../vendor/GRDB.swift
targets:
  TodoFocusMac:
    type: application
    platform: macOS
    sources:
      - Sources
      - path: Assets.xcassets
        buildPhase: resources
    settings:
      base:
        PRODUCT_NAME: TodoFocusMac
        INFOPLIST_FILE: Info.plist
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        CODE_SIGN_IDENTITY: "Apple Development: mjh5311@qq.com (29GMPG4CQB)"
        DEVELOPMENT_TEAM: ""
        CODE_SIGN_STYLE: Manual
    dependencies:
      - package: GRDB
    scheme:
      testTargets:
        - CoreTests
        - DataTests
  CoreTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests/CoreTests
    dependencies:
      - target: TodoFocusMac
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        BUNDLE_LOADER: "$(TEST_HOST)"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/TodoFocusMac.app/Contents/MacOS/TodoFocusMac"
  DataTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests/DataTests
    dependencies:
      - target: TodoFocusMac
      - package: GRDB
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        BUNDLE_LOADER: "$(TEST_HOST)"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/TodoFocusMac.app/Contents/MacOS/TodoFocusMac"
