# Zen Todo App Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a minimalist, calming, full-featured local Todo application with satisfying interactions and zero clutter.

**Architecture:** A unified Next.js App Router application using Server Actions for data mutation, communicating with a local SQLite database via Prisma ORM.

**Tech Stack:** Next.js (App Router), Tailwind CSS, Framer Motion, Prisma, SQLite, Lucide-React.

---

## Chunk 1: Initialization & Database Setup

### Task 1: Project Scaffolding

**Files:**
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `next.config.mjs`
- Create: `tailwind.config.ts`
- Create: `src/app/layout.tsx`
- Create: `src/app/page.tsx`
- Create: `src/app/globals.css`

- [ ] **Step 1: Initialize Next.js project with Tailwind**

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes
```

- [ ] **Step 2: Install additional dependencies**

```bash
npm install framer-motion lucide-react clsx tailwind-merge
npm install -D prisma
```

- [ ] **Step 3: Setup basic Zen global styles**

Modify `src/app/globals.css`:
- Remove default Next.js boilerplate css.
- Add a soft, calming background color (e.g., `#fafafa` or `#fcfcfc`).
- Set text color to a soft charcoal (e.g., `#333`).

- [ ] **Step 4: Commit**

```bash
git add .
git commit -m "chore: initialize next.js project with tailwind and framer-motion"
```

### Task 2: Database & Prisma Setup

**Files:**
- Create: `prisma/schema.prisma`
- Create: `src/lib/db.ts`

- [ ] **Step 1: Initialize Prisma with SQLite**

```bash
npx prisma init --datasource-provider sqlite
```

- [ ] **Step 2: Define the Todo schema**

Modify `prisma/schema.prisma` to include a `Todo` model:

```prisma
model Todo {
  id          String   @id @default(cuid())
  title       String
  isCompleted Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
```

- [ ] **Step 3: Run database migration**

```bash
npx prisma migrate dev --name init
```

- [ ] **Step 4: Create Prisma client singleton**

Create `src/lib/db.ts` with standard Prisma global initialization to prevent connection limit issues in Next.js dev mode.

```typescript
import { PrismaClient } from '@prisma/client'

const globalForPrisma = global as unknown as { prisma: PrismaClient }

export const prisma =
  globalForPrisma.prisma ||
  new PrismaClient({
    log: ['query'],
  })

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

- [ ] **Step 5: Commit**

```bash
git add prisma/ src/lib/ .env
git commit -m "feat: setup sqlite database and prisma schema"
```

---

## Chunk 2: Backend Logic & Server Actions

### Task 3: Todo Server Actions

**Files:**
- Create: `src/actions/todos.ts`

- [ ] **Step 1: Create fetch action**

Implement `getTodos()` in `src/actions/todos.ts` fetching all todos from Prisma, ordered by `isCompleted` (false first) and `createdAt` (descending).

- [ ] **Step 2: Create add action**

Implement `addTodo(formData: FormData)`:
- Validate input.
- Use Prisma `create`.
- Call `revalidatePath('/')`.

- [ ] **Step 3: Create toggle action**

Implement `toggleTodo(id: string, isCompleted: boolean)`:
- Use Prisma `update`.
- Call `revalidatePath('/')`.

- [ ] **Step 4: Create delete action**

Implement `deleteTodo(id: string)`:
- Use Prisma `delete`.
- Call `revalidatePath('/')`.

- [ ] **Step 5: Commit**

```bash
git add src/actions/
git commit -m "feat: implement server actions for todo crud operations"
```

---

## Chunk 3: Frontend Implementation

### Task 4: Zen UI Components (Input)

**Files:**
- Create: `src/components/TodoInput.tsx`

- [ ] **Step 1: Build the TodoInput component**

Create `src/components/TodoInput.tsx`:
- Use `"use client"`.
- Minimalist text input field with no visible borders until focused.
- Use `useFormStatus` to handle loading state (disable input while adding).
- Handle form submission calling the `addTodo` server action.

- [ ] **Step 2: Commit**

```bash
git add src/components/TodoInput.tsx
git commit -m "feat: add minimalist todo input component"
```

### Task 5: Zen UI Components (List & Items)

**Files:**
- Create: `src/components/TodoList.tsx`
- Create: `src/components/TodoItem.tsx`

- [ ] **Step 1: Build TodoItem with Framer Motion**

Create `src/components/TodoItem.tsx`:
- Use `"use client"`.
- Props: `todo` object.
- Use `<motion.div>` for layout animations (`layout` prop).
- Implement custom circular checkbox (svg) that calls `toggleTodo`.
- Implement delete icon (Lucide `Trash2` or `X`) that appears on hover and calls `deleteTodo`.
- Use `useTransition` for immediate UI feedback.

- [ ] **Step 2: Build TodoList container**

Create `src/components/TodoList.tsx`:
- Props: `initialTodos`.
- Render an `AnimatePresence` list.
- Map over todos and render `TodoItem`s.

- [ ] **Step 3: Commit**

```bash
git add src/components/
git commit -m "feat: add animated todo list and item components"
```

### Task 6: Assembly & Polish

**Files:**
- Modify: `src/app/page.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Update main page layout**

Modify `src/app/page.tsx` (Server Component):
- Import `getTodos`.
- Fetch data: `const todos = await getTodos()`.
- Render `TodoList` passing `todos`.
- Wrap in a layout with maximum whitespace: a clean, centered column (e.g., `max-w-xl mx-auto mt-24`).
- Add a minimalist header (e.g., "focus." or "zen.") with elegant typography.

- [ ] **Step 2: Verify and Test**

Run `npm run dev` and verify:
- Adding tasks works.
- Completing tasks toggles state and animation.
- Deleting tasks removes them with animation.
- Persistence works after refresh.

- [ ] **Step 3: Commit**

```bash
git add src/app/
git commit -m "feat: assemble main page and polish zen layout"
```
